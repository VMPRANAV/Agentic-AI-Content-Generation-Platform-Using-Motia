# Database Configuration
# PostgreSQL connection string - REQUIRED for the application to work
# Format: postgresql://username:password@host:port/database_name
# Example: postgresql://user:password@localhost:5432/content_creation_db
DATABASE_URL=postgresql://username:password@localhost:5432/your_database_name

# PostgreSQL SSL Mode (optional)
# Set to 'require' for hosted PostgreSQL services like Supabase, Railway, etc.
# Leave empty for local development without SSL
PGSSLMODE=require

# Notification Configuration
# Email address used for sending content completion notifications
# Used by the cron job that sends emails when content is ready
# Defaults to 'user@example.com' if not set
DEFAULT_NOTIFICATION_EMAIL=your-email@example.com
GROQ_API_KEY=
TAVILY_API_KEY=
# Development/Production Environment
# Set to 'production' for production deployments
# Used by various libraries for optimization and error handling
NODE_ENV=development
